@page "/profile"

<MudContainer Class="profile-container">
    <MudText Typo="Typo.h4" Class="profile-title">User Profile</MudText>
    <MudForm @ref="Form" Class="profile-form">
        <MudTextField @bind-Value="FirstName" Label="First name" Variant="Variant.Outlined" Class="profile-field" />
        <MudTextField @bind-Value="LastName" Label="Last name" Variant="Variant.Outlined" Class="profile-field" />
        <MudTextField @bind-Value="Email" Label="Email" Variant="Variant.Outlined" Class="profile-field" />
        <MudButton OnClick="SaveProfile" Color="Color.Primary" Variant="Variant.Filled" Class="profile-button">Save</MudButton>
    </MudForm>
</MudContainer>

@code {
    protected MudForm? Form;
    protected string? FirstName { get; set; }
    protected string? LastName { get; set; }
    protected string? Email { get; set; }

    protected void SaveProfile()
    {
        Console.WriteLine($"Saved profile : {FirstName} {LastName} ({Email})");
    }
}
